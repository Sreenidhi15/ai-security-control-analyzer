# AI-Powered Security Control Analyzer — Dependencies
# Install with: pip install -r requirements.txt

# Core
pydantic>=2.0
python-dotenv>=1.0

# Windows scanning (Phase 2) — Windows only
pywin32>=306; sys_platform == "win32"
ldap3>=2.9

# LLM + Agentic (Phases 3, 5, 6)
openai>=1.30
langchain>=0.2
langchain-openai>=0.1
langchain-community>=0.2

# PDF generation (Phase 6)
reportlab>=4.0

# Database / utilities
# sqlite3 is stdlib — no install needed
tabulate>=0.9
rich>=13.0

# Testing
pytest>=8.0
pytest-mock>=3.12
